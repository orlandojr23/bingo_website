export const metadata = {
  title: "Driver Sign In",
  description: "Sign in to the Bin'Go driver terminal for Barangay Tejero.",
};

export default function DriverLoginLayout({ children }) {
  return children;
}
