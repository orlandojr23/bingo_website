export const metadata = {
  title: "Forgot Password",
  description: "Reset your Bin'Go resident account password.",
};

export default function ForgotPasswordLayout({ children }) {
  return children;
}
