export const metadata = {
  title: "About",
  description:
    "Bin'Go was built to connect residents with their barangay so garbage collection is clear, on time, and easy for everyone.",
};

export default function AboutLayout({ children }) {
  return children;
}
